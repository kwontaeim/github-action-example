name: Build and deploy React App

on:
  pull_request:
    branches: [master]
  push:
    branches: [master]

jobs:
  test:
    runs-on: "ubuntu-latest"
    steps:
      - name: Print Hello World
        run: echo "Hello, World"
